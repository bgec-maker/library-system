# 70 · search-filter-chips — 검색 필터 활성 칩 표시

배경: 셀렉트 3개는 접힌 상태에서 무엇이 걸려 있는지 안 보인다 — 결과가 이상하게 적을 때
사서가 원인을 못 찾는다.

할 일: 활성 필터를 결과 위 칩(라벨 + X)으로 표시, X로 개별 해제, 전체 해제 유지. 칩은 v2 표면
규칙(톤 배경), 44px 터치.

완료 조건: 캡처 1장, 전 게이트.
