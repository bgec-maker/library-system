# 62 · dock-badge-parity — 데스크톱 도크 등록 아이콘 실패 배지 (53 패리티)

배경: 모바일 탭 배지(53)와 달리 데스크톱 도크는 등록 실패를 표시하지 않는다 — 같은 신호는
두 셸에서 같아야 한다(FRONTEND 제1원칙).

할 일: Dock 등록 아이콘에 --fail 배지(개수, 9+ 캡), registerQueue 구독(useSyncExternalStore),
sr-only 문구, 미니마이즈 트레이와 겹침 없는 위치. 60의 부팅 재개로 셸 부팅 시 모듈은 이미 적재됨.

완료 조건: 시딩 캡처 1장, 전 게이트.
