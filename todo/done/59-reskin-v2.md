# 59 · reskin-v2 — 표면 시스템 리스킨 + 배치 정리 (시안 B + 따뜻한 캔버스, 사용자 채택)

시안 비교(3안 실렌더) 후 사용자 확정: "추천대로(B 구조+웜 캔버스) + 버튼·섹션 배치 함께".
DESIGN.md 「v2 표면 시스템」이 법 — 이 항목은 그 이행.

표면: --radius 10, --shadow-card(-up)·--deep-tint 토큰 신설(양 스킨), .panel·DataTable 카드
무보더+그림자, 탭바 보더→상향 그림자, 셸/스택 헤더 딥 네이비(타이틀·백·설정 = --panel),
lr-slot 대시 보더→톤 카드(빈=tint, 참=화이트+pass 보더+그림자), 창 본문 paper 캔버스.

배치: 뷰 내부 h1 sr-only(이중 타이틀 해소, 접근성 헤딩 유지 — lr·reg), reg 헤더=메타 행으로,
scan-off-card의 panel 클래스 제거(카드-안-카드 해소), 등록 보조 진입 2-up 행(.reg-scanActions),
reg-root 간격 토큰화. 히어로 버튼 warn→기본 프라이머리(카메라 시작·점검 시작).

## 완료 조건: 모바일·데스크톱 비포/애프터 컷 · 전 게이트·e2e 5스펙 · DESIGN.md 반영
