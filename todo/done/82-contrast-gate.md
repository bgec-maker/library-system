# 82 · contrast-gate — 색 대비 검사 게이트

배경: 토큰 조합(ink-3 on paper 등)의 WCAG 대비를 눈대중으로 지켜왔다 — 게이트로 굳히면
리스킨류 작업이 안전해진다.

할 일: scripts/check-contrast.mjs — tokens/*.css 파싱, 지정 전경/배경 쌍 목록의 대비율 계산,
4.5:1(본문)/3:1(대형·아이콘) 미달 시 실패. verify에 편입.

완료 조건: verify 체인 포함, 현행 토큰 전부 통과(미달 발견 시 토큰 보정 포함).

## 이행 노트 — 게이트 첫 실행이 11건 적발(눈대중의 실체)
- work: --ink-3(2.87:1)·--wait(4.23:1) 보정 → #666c78·#7a6c40 (색상 계열 유지, 명도만).
- student: --ink-3·--pass·--fail 보정. --wait(앰버)·--brass 쌍은 학생 표면에 해당 조합이
  실존하지 않아(도크·샘플 배지·warn 버튼 = 사서 셸 전용) 스킨별 쌍 목록으로 정리 — "존재하는
  조합만 검사"가 이 게이트의 규약(새 조합을 쓰면 목록에도 추가).
- verify 체인 편입(check-perf-budget 다음). DESIGN.md 토큰 절의 값 갱신은 별도 없음 — 표의
  대비 원칙이 이 게이트로 실행됨.
