# VERIFY.md — 통합 웹앱(webapp/) 검증 절차

> 🔄 **2026-07-15 갱신 (todo/07 통합 체크포인트)** — todo/02~06 반영: 카메라 온디맨드(더 이상 상시 구동
> 아님) · 대시보드 기저층 · 리포트 R1(인쇄) · 시각화 V1 4종 · 다국어 토글 · 번들 예산 CI. 아래 절차를
> 최신 동작 기준으로 다시 썼습니다 — **1.3절(스캐너)이 특히 바뀌었으니 예전 기억으로 건너뛰지 마세요.**

이 문서는 실제 기기·실제 GAS 배포로만 확인 가능한 것을 검증하는 절차입니다. 이 환경(코드 작성 세션)에는
GAS 런타임도, 실제 아이폰도, 화면이 있는 브라우저도 없습니다. 여기서는 **정적 검증(lint·타입체크·빌드·
번들 예산·비밀값 grep)까지만** 매 항목(todo/NN)마다 자동으로 수행했고, 아래 절차는 **사용자가 실제
폰·데스크톱 브라우저로 수행**해야 합니다 — school-patch-v1의 9단계 검증 프로토콜과 같은 성격입니다.

## 이미 확인된 것 (todo/01~06, 매 항목 커밋 전 자동으로)

| 항목 | 방법 | 결과 |
|---|---|---|
| `views/**` 셸 접근 금지 (matchMedia/innerWidth/window.\*/셸 import) | ESLint 커스텀 룰 + 독립 grep 스크립트(`npm run check-view-boundary`) 이중 방어 | 0건 |
| `views/**·shells/**·student/**` JSX 한글 리터럴 금지 (ADR-023) | ESLint 커스텀 룰 + `npm run check-i18n-literals` 이중 방어 | 0건 |
| 타입 정합성 | `tsc -b --noEmit` | 오류 0건 |
| 전체 린트 | `eslint . --max-warnings 0` | 오류·경고 0건 |
| 프로덕션 빌드 | `npm run build` | 성공 |
| **번들 예산** (work ≤180KB / student ≤70KB gzip) | `npm run size`(`.vite/manifest.json` 그래프 분석, CI 게이트) | **work(desktop 기준) 64.8KB · student 51.1KB — 여유 있음. 아래 "번들 예산 실측 기록" 참고** |
| `getUserMedia` 호출 지점 | `camera.ts` 1곳만 (grep으로 확인) | 1곳 |
| Code.gs 보호 로직·기존 함수 무결성 | `checkout_`/`return_`/`executeWrite_`/`ensureSchema_`/`protectDatabaseSheets_`/`dailyLibraryMaintenance` 등 매 항목마다 재확인 grep + `node --check` 구문 검증 | 전부 무수정 확인 |
| 비밀값(`ttbbgec`/`alansgk`) 미커밋 | 매 항목 커밋 전 전체 트리 grep | 0건(규칙 설명 문구 자체 제외) |
| 학생 번들 분리 | 헤드리스 브라우저로 `#/b/0001234` 방문 → 네트워크 요청에 `DesktopShell-*.js`/`MobileShell-*.js` 없음 확인 | 통과 |
| 데스크톱: 창 드래그·최소화·닫기·도크 클릭으로 창 열기 | 헤드리스 브라우저로 클릭·드래그 시뮬레이션 | 통과 |
| 창 위치 영속 | 드래그 후 `localStorage['win:'+viewId]` 기록 확인 → 새로고침 후 유지 | 통과 |
| 모바일: 탭바 렌더, 콘솔 에러 없음 | 헤드리스 브라우저(390×844 뷰포트) | 통과 |
| 세션 게이트(초기 설정 화면) | 헤드리스 브라우저로 입력→저장→셸 노출, 톱니바퀴로 재오픈 확인 | 통과 |

**확인 못 한 것 — 아래 절차로 사용자가 확인해야 함**: 실제 카메라·ZXing 디코드, 카메라 온디맨드 정책의
실사용감(유휴 타이머·연속 모드), iOS Safari 설치형 PWA 동작, 실제 대출·반납/등록·대시보드·리포트·시각화가
시트 데이터와 실제로 맞는지, 인쇄 미리보기의 실제 A4 출력, 창 리사이즈 8방향 핸들의 실제 사용감, 모바일 탭
전환 애니메이션, 좌우 스냅, 최대 6개 창 제한의 실제 UX, 언어 토글의 실제 전환감.

**⚠️ 알려진 스코프 축소 — CLAUDE.md 규칙 7 부분 미충족**: 규칙 7은 "ZXing 로컬 번들 + Worker + 크롭 +
10fps 스로틀"을 요구합니다. `camera.ts`는 이 중 **로컬 번들·크롭·10fps 스로틀 셋만** 구현했고 **Web Worker
오프로딩은 넣지 않았습니다**(메인 스레드에서 디코드) — register.html 때와 동일한 트레이드오프이며
`school-patch-v1/PATCH_NOTES_MOBILE_REG.md`에도 같은 내용이 기록되어 있습니다. 2절의 "20권 연속 스캔"에서
발열·버벅임이 실제로 문제라면, 다음 개선 항목은 ZXing 디코드를 Worker로 옮기는 것입니다(현재는 크롭으로 처리
픽셀을 줄여 완화만 함).

## 번들 예산 실측 기록 (todo/07)

`npm run size`가 `.vite/manifest.json`의 정적 import 그래프를 따라가 계산한 실측치(gzip)입니다. 항목이
추가될 때마다 이 표에 최신 수치를 남깁니다 — 예산 초과는 CI가 빌드 실패로 막지만, 여유가 줄어드는 추세
자체는 이 표로만 보입니다.

| 시점(todo 완료 직후) | work · desktop | work · mobile | student | 예산 |
|---|---|---|---|---|
| todo/02 (perf-ci+i18n 기반) | 58.4KB | 56.6KB | 50.8KB | work 180KB / student 70KB |
| todo/03 (camera-on-demand) | 59.3KB | — | 50.8KB | 〃 |
| todo/04 (dashboard-baselayer) | 63.7KB | — | 51.1KB | 〃 |
| todo/05 (reports-r1) | 64.2KB | — | 51.1KB | 〃 |
| todo/06 (viz-v1) | 64.8KB | 59.4KB | 51.1KB | 〃 |

work는 desktop·mobile 중 더 큰 쪽을 예산 판정에 쓴다(`check-bundle-size.mjs`, docs/ASSUMPTIONS.md
todo/02 참고). 두 셸 모두 예산의 3분의 1 남짓만 쓰고 있어 todo/08(공용 DataTable+catalog)·todo/09(R1
잔여+en 완역) 이후에도 여유가 있을 것으로 예상되지만, todo/08의 catalog 미러(IndexedDB, 5,000행 규모)가
가장 큰 변수이므로 그 항목 완료 직후 반드시 재실측할 것.

## 0. 사전 준비

1. **GAS 백엔드는 이미 배포되어 있다고 가정합니다만, 이번 라운드(todo/04~06)가 `doPost`에 `dashboard`·
   `report`·`viz` 세 읽기 액션을 새로 추가했습니다** — 재배포 전까지 이 셋은 전부 `UNKNOWN_ACTION`을
   반환하고, 웹앱은 이를 정상적인 "아직 배포 전" 상태로 인식해 **자동으로 샘플 데이터 + "샘플 데이터" 배지**로
   보여줍니다(아래 "4. 백엔드 재배포"절 참고). Web App URL, `MOBILE_REG_TOKEN`, 작업자 이름을 준비하세요 —
   `school-patch-v1/PATCH_NOTES_MOBILE_REG.md`의 "사전 준비 (설치 순서)" 참고.
2. `.github/workflows/pages.yml`이 `main` 브랜치 push마다 `webapp/`을 빌드해 Pages의 `/app/` 경로에,
   `spike/`를 그대로 사이트 루트에 병행 배포합니다. 즉:
   - 기존 스파이크·등록 도구: `https://bgec-maker.github.io/library-system/` (그대로 살아있음)
   - 새 통합 웹앱: `https://bgec-maker.github.io/library-system/app/`
3. push 후 GitHub Actions "Deploy to GitHub Pages" 워크플로가 성공했는지 Actions 탭에서 확인하세요.
   `웹앱 뷰 경계·i18n 린트` 또는 `번들 예산 체크` 스텝이 실패하면 배포 자체가 안 됩니다(의도된 CI 게이트).

## 1. 데스크톱 브라우저에서 (노트북/PC)

1. `/app/`을 열면 **초기 설정 화면**이 뜹니다 — GAS Web App URL·공유 토큰·이름(작업자)을 입력하고 저장하세요.
   나중에 좌측 도크 맨 아래 ⚙로 다시 열 수 있습니다.
2. **창을 전부 닫은 상태에서 워크스페이스를 보세요** (todo/04):
   - [ ] 창이 하나도 없으면 **대시보드**가 바탕에 항상 보인다(닫기·이동 대상이 아님 — 다른 창들과 달리
     도크에도 별도 아이콘이 없다, 그냥 항상 거기 있음)
   - [ ] KPI 카드 6개(대출중·오늘반납예정·가용소장본·예약대기·활성회원·연체)에 숫자가 보인다(재배포 전이면
     "샘플 데이터" 배지와 함께 그럴듯한 표본 숫자)
   - [ ] 「조용한 신호」 패널의 리포트 5줄 중 아무거나 클릭 → `reports` 창이 그 종류로 바로 열린다("직행")
   - [ ] 「연체 상위」·「최근 처리」 패널에 각각 데이터/링크가 보인다
   - [ ] 우측 상단 "새로고침" 버튼을 누르면 재조회되고(로딩 표시), "최근 갱신: HH:MM:SS"가 갱신된다
   - [ ] 창을 하나 열어 앞을 가려도 대시보드는 그 뒤에 그대로 있다가, 창을 닫으면 다시 보인다(창이 겹쳐도
     대시보드가 위로 올라오거나 밀려나지 않는지 — z-순서 확인)
3. **창 관리자**: 좌측 도크 아이콘을 클릭해 "대출·반납"과 "도서 등록"을 각각 열어보세요.
   - [ ] 타이틀바를 드래그하면 창이 움직인다
   - [ ] 모서리를 드래그하면 리사이즈된다 (최소 크기 아래로는 안 줄어든다)
   - [ ] 최소화하면 도크 하단에 아이콘으로 쌓이고, 클릭하면 복원된다
   - [ ] 좌/우 스냅 버튼을 누르면 화면 절반을 차지한다
   - [ ] 창을 닫았다 같은 아이콘을 다시 클릭하면(single 뷰) 새 창이 아니라 **같은 창이 포커스**된다(중복 생성 안 됨)
   - [ ] 창을 7개째 열려고 하면 토스트로 거부된다(최대 6개)
   - [ ] 브라우저를 새로고침해도 창 위치·크기가 그대로 복원된다
4. **스캐너 — 이제 기본 꺼짐입니다 (ADR-020, todo/03으로 정책이 뒤집혔습니다)**: 우하단 스캐너 도크
   위젯이 처음엔 **"카메라 시작" 버튼만** 보이고 프리뷰는 없습니다. 세 가지 시작 방법을 각각 확인하세요:
   - [ ] **위젯 클릭**: 위젯의 "카메라 시작" 버튼을 누르면 브라우저가 카메라 권한을 요청하고(허용), 프리뷰가
     뜬다(macOS/Windows Chrome이면 BarcodeDetector 네이티브, 그 외엔 ZXing)
   - [ ] **뷰 버튼**: 카메라를 끈 상태에서 "대출·반납"(또는 "도서 등록"·"장서 점검") 창을 열면 그 화면 안에도
     "카메라 시작" 버튼이 보이고, 눌러도 똑같이 켜진다
   - [ ] **단축키 S**: 입력창(텍스트 필드)에 포커스가 없는 상태에서 키보드 `S`를 누르면 켜지고, 다시 누르면
     꺼진다. **입력창 안에서 `S`를 타이핑할 땐 아무 반응이 없어야 한다**(폼 입력 방해 금지 확인)
   - [ ] **유휴 자동 종료**: 스캔 없이 약 2분 50초 기다리면 "곧 카메라가 꺼집니다" 토스트가 뜨고, 10초 뒤
     실제로 꺼진다(총 유휴 3분)
   - [ ] **연속 모드 핀**: 카메라가 켜진 상태에서 "연속 모드" 체크박스를 켜면, 그 이후로는 유휴 3분이 지나도
     꺼지지 않는다(러시아워 대비). 체크 해제하면 그 시점부터 다시 3분 카운트다운이 시작된다
   - [ ] 위젯 접기 버튼을 눌러도(카메라가 켜진 상태에서) 스트림 자체는 안 꺼진다(다시 펼치면 바로 프리뷰가
     보임 — 껐다 켜는 지연 없음). **접기와 카메라 on/off는 별개**
   - [ ] 창을 전부 닫아도 스캐너 위젯(과 켜져 있던 카메라 상태)은 그대로 살아있다("카메라는 창이 아니다")
5. **스캔 라우팅**: 카메라를 켠 채로 "대출·반납"과 "도서 등록" 창을 동시에 띄우고 번갈아 클릭(포커스 전환)
   하면서 책 바코드를 스캔:
   - [ ] 포커스된 창으로 스캔이 간다 (다른 창은 반응 없음)
   - [ ] 핀 아이콘을 누르면 포커스를 옮겨도 핀 고정된 창으로 계속 스캔이 간다
   - [ ] 두 창을 모두 닫은 상태에서 스캔하면 "대출·반납" 창이 자동으로 열린다
6. **리포트 R1 + 인쇄** (todo/05): 대시보드 「조용한 신호」에서 "미대출 학생 발굴"을 클릭(또는 도크에서
   `reports` 직접 열기):
   - [ ] 미대출 학생 발굴 미리보기 → 반별 이름 명단이 보인다(숫자만이 아니라 실제 이름)
   - [ ] 담임 리포트 → 학년·반·월 입력 → 미리보기에 대출 현황·미대출 명단·연체·인기책이 보인다
   - [ ] "인쇄" 버튼 → 브라우저 인쇄 미리보기가 **A4 세로 1장**으로 뜬다(학교명·생성일 머리, 페이지 번호
     꼬리). 화면의 창 테두리·도크·다른 창들이 인쇄 미리보기에 안 나온다(리포트 내용만)
   - [ ] 나머지 3종(죽은 장서·회수 쪽지·기증 감사장) 카드는 "다음 항목에서 구현됩니다" 표시만 있고 죽은
     버튼처럼 아무 반응 없이 방치되진 않는다(todo/09 대기 상태임을 명확히 알 수 있는지)
7. **시각화 V1** (todo/06): 대시보드에서 대출 잔디(캘린더 히트맵)·예약 압력(스파크라인)이 보이고, `reports`
   허브의 "장서 시각화" 카드에서 카테고리 트리맵·회전율 사분면이 보이는지:
   - [ ] 색이 전부 문서의 램프 톤(paper→deep 계열 또는 범주색)이고 임의로 튀는 색(예: 밝은 빨강/파랑
     원색)이 안 보인다
   - [ ] 각 차트 아래 "표로 보기"(또는 이미 표로 보이는) 대체 표현이 있다
   - [ ] 차트를 처음 로드할 때 전체 페이지가 느려지지 않는다(스크롤/탭 전환으로 화면에 들어올 때 그려지는
     느낌인지)

## 2. 아이폰 — 설치형 PWA (진짜 시험)

**브라우저 탭에서 되는 건 이미 압니다. 설치형에서 되는지가 관건입니다** (스파이크 때와 같은 원칙).

1. Safari로 `https://bgec-maker.github.io/library-system/app/` 접속 → **공유 → 홈 화면에 추가** → 그 아이콘으로 다시 열기.
2. 초기 설정 화면에서 URL·토큰·이름 입력.
3. 하단 탭바가 보이고 **첫 탭이 "스캔"(=대출·반납 뷰)** 인지 확인.
4. **카메라 온디맨드 — 모바일**(todo/03): 스캔 탭에 들어가도 카메라가 자동으로 켜지지 않고 **"카메라 시작"
   버튼**이 보이는지 확인. 버튼을 눌러 카메라를 켠 뒤:
   - [ ] ISBN 스캔 20권 연속 시도 — 연속으로 인식되는지(한 번 읽고 멈추면 실패), 발열·크래시 없는지,
     부가기호(ISBN 옆 5자리 바코드)를 스캔했을 때 무시되고 계속 대기 상태인지
   - [ ] 스캔 탭에서 다른 탭(검색·더보기 등)으로 전환하면 **유예 없이 즉시** 카메라가 꺼지는지(데스크톱의
     유휴 3분 유예와 다름 — 모바일은 이탈 즉시 종료가 정책)
   - [ ] "더보기" 탭에서 "연속 모드" 토글을 켠 뒤 스캔 탭으로 돌아가 시작 → 다른 화면에서 다시 돌아와도(탭
     이탈로 꺼졌다가) 여전히 버튼을 눌러야 다시 켜지는지 확인(연속 모드는 "이탈 시 종료"까지 막지는
     않음 — 유휴 타이머만 막는다는 설계임을 이해하고 확인)
5. "더보기" 탭에서 "장서 점검"(inventory, scan:focus)에 들어가서 "카메라 시작"을 눌러야 켜지는지 확인.
6. **언어 토글** (ADR-023): "더보기" 탭 상단의 KO/EN 버튼을 눌러 화면 문구가 영어로 바뀌는지, 다시 KO를
   누르면 한국어로 돌아오는지. 앱을 완전히 종료 후 재실행해도 마지막에 고른 언어가 유지되는지.
7. 뒤로가기 제스처: 도서 상세 같은 push 화면에서 뒤로가기 → 이전 화면으로. 탭 루트에서 뒤로가기 → 앱이 죽지 않고
   탭이 유지되는지 확인(뒤로가기가 Safari를 벗어나거나 PWA를 종료하면 안 됨).
8. **리포트 인쇄(모바일)**: "더보기" → 리포트 아무거나 미리보기 → 공유 시트의 "프린트"를 눌러 인쇄 미리보기가
   A4 형태로 뜨는지(에어프린트 대상 프린터가 없어도 미리보기 화면까지는 확인 가능).
9. **🔥 iOS 설치형 1권 왕복** (H3 긴급 수정 확인, 2026-07-15 — "서버는 4~5초에 완료했는데 폰 fetch가
   ~500ms에 TypeError 'Load failed'" 버그의 실기기 재확인): 반드시 **홈 화면 아이콘으로 실행한 설치형
   PWA**에서(Safari 탭이 아니라) 아래를 확인하세요 — Safari 탭/PC는 원래도 정상 추정이라 이 버그를
   재현하지 않습니다.
   - [ ] 대출·반납 탭에서 실제 책 1권을 스캔해 **대출(또는 반납) 1건**을 끝까지 완료한다 — 이전에
     "Load failed"로 실패하던 흐름이 이번엔 성공하는지(또는 최소한 아래 GET 폴백으로 성공하는지) 확인.
   - [ ] 더보기(또는 대시보드) 탭에서 **대시보드 새로고침 1회**를 실행해 KPI가 갱신되는지 확인(읽기
     액션 왕복 확인 — `dashboard`는 GET 폴백 대상 action 중 하나).
   - [ ] 만약 POST가 여전히 "Load failed"로 실패한다면: "도서 등록" 탭의 **"진단 로그 보기"**를 열어
     같은 requestId로 두 줄(POST 실패 → GET 재시도)이 나란히 보이는지, GET 재시도 줄의 결과가
     `ok`인지 확인하고, 두 줄 모두(특히 실패한 POST 줄의 `err: TypeError` · `res.type` ·
     `redirected` 값) 스크린샷/복사해서 알려주세요 — 이 필드들이 다음 진단의 핵심 단서입니다.
   - [ ] 대출·반납·등록처럼 **쓰기 액션**은 GET으로 재시도되지 않는다는 점을 기억하세요 — POST가
     "Load failed"로 죽으면 그 요청은 그대로 실패로 남고(멱등 규약대로 같은 requestId로 사람이
     수동 재시도), 화면에 오류가 뜨는 게 정상입니다(자동으로 조용히 GET으로 흘러가지 않음).

## 3. 🔴 스모크 버그 진단 — "스캔·저장까지 갔는데 시트에 안 보임"

register.html에서 미해결로 남아있던 버그입니다. 새 앱(`views/register`)도 **같은 doPost**를 쓰므로 여기서
재현될 수 있습니다. 이번에 진단 전용 UI를 register 뷰에 넣었으니 아래 순서로 원인을 좁히세요.

1. "도서 등록" 탭(또는 창) 헤더의 **"진단 로그 보기"**를 눌러 패널을 엽니다.
2. 평소처럼 ISBN 스캔 → 확인 폼 → 저장을 진행합니다.
3. 저장 직후 화면에 **등록번호와 함께 "요청 ID: xxx"**가 표시됩니다. 이 값을 적어두세요(또는 "진단 로그 복사"로
   전체를 클립보드에 복사).
4. 진단 로그 패널에서 방금 요청(`registerByIsbn`)의 줄을 찾아 다음을 확인:
   - **outcome이 'ok'가 아니면** → 프론트가 실패를 감지한 것입니다. `resultCode`/`resultMessage`를 그대로
     스크린샷하거나 복사해 알려주세요 (프론트 문제일 가능성 — 예: `NETWORK_ERROR`/`CLIENT_TIMEOUT`이면
     요청이 서버에 아예 안 닿았거나 응답을 못 받은 것).
   - **outcome이 'ok'인데 시트에 안 보이면** → 요청은 서버까지 갔고 서버도 성공을 응답한 것입니다. 이제
     서버 쪽을 봐야 합니다:
     a. Apps Script 편집기 → **실행 기록(Executions)**에서 저장 시각 근처의 `doPost` 실행을 찾아 로그를 확인하세요.
     b. 스프레드시트에서 **`18_SYS_OPERATIONS`** 시트(기본은 숨김 — 메뉴에서 잠깐 보이게 하거나 시트 목록에서
        직접 선택)를 열어 4단계에서 적어둔 requestId로 검색(Ctrl+F)하세요.
        - 행이 아예 없다 → 요청이 **다른 GAS 배포/다른 시트**로 갔을 가능성이 큽니다. 초기 설정에 입력한
          Web App URL이 지금 보고 있는 스프레드시트에 바인딩된 배포가 맞는지 다시 확인하세요(여러 번 배포하면서
          URL이 여러 개 생겼을 수 있습니다 — 가장 최근 활성 배포 URL인지 확인).
        - `status_code`가 `COMPLETED`다 → 서버는 정상적으로 다 처리했다고 기록했습니다. 이 경우 `08_COPIES`
          시트(역시 기본 숨김)에서 응답에 찍힌 barcode로 검색하세요. **여기서도 없으면 진짜 서버 버그**입니다
          — `registerByIsbn`/`registerTitle_`/`registerCopy_` 쪽을 봐야 합니다.
        - `status_code`가 `STARTED`에 멈춰 있다 → 실행이 중간에 죽은 것입니다(타임아웃 등). 무결성 점검
          메뉴를 실행해보세요.
        - `status_code`가 `FAILED`다 → `error_message` 열에 실패 사유가 있습니다.
   - 화면엔 보이는데 사용자가 못 찾았을 가능성도 있습니다 — `03_TITLES`/`08_COPIES`는 **기본적으로 숨김
     시트**입니다(사서가 실수로 정렬·수정하지 못하게 하는 설계, `docs/DECISIONS.md` ADR-006). 운영센터
     대시보드 숫자도 "새로고침" 메뉴를 눌러야 갱신됩니다 — 자동 갱신 아님.
5. 위 단서(진단 로그 outcome, requestId, 18_SYS_OPERATIONS 상태, 08_COPIES 존재 여부)를 모아서 알려주시면
   프론트/서버 어느 쪽이 원인인지 바로 좁혀집니다.

## 4. 백엔드 재배포 — 지금 필요합니다 (⚠️ 건너뛰면 대출·반납·대시보드·리포트·시각화가 전부 샘플로만 보입니다)

`school-patch-v1/Code.gs`의 `doPost`에 지금까지 **7개 웹앱 전용 액션**이 쌓였습니다: `copyStatus` /
`checkout` / `return`(A트랙, todo 이전) · `dashboard`(todo/04) · `report`(todo/05) · `viz`(todo/06). 전부
기존 `executeWrite_`/`checkout_`/`return_`/`getDashboardData_`/`readTable_` 재사용, 보호 로직 무수정.

**재배포 전 상태(정상입니다, 버그 아님)**: 위 신규 액션들은 `UNKNOWN_ACTION`을 반환합니다. 웹앱은 이를
감지해 **자동으로 샘플 데이터 + "샘플 데이터" 배지**를 보여줍니다(대시보드 KPI·조용한 신호 결과·리포트
미리보기·시각화 차트 전부 동일 패턴). 대출·반납만은 폴백이 없으므로 재배포 전엔 스캔해도 동작하지 않습니다
("서버 배포가 이전 버전입니다 — 재배포하세요" 오류로 표시).

이게 동작하려면 **Apps Script 재배포가 필요합니다**:
1. Apps Script 편집기에서 `school-patch-v1/Code.gs` 최신본으로 교체 (스코프 변경 없어 재승인은 안 뜸).
2. **배포 → 배포 관리 → 연필 아이콘 → 버전: "새 버전" → 배포**. (⚠️ "새 배포"를 만들면 URL이 바뀌어 모든
   기기의 설정을 다시 입력해야 합니다 — 반드시 기존 배포를 **새 버전으로 갱신**하세요. URL 불변.)
3. (todo/06 전용) **일일 시각화 배치를 한 번 더 설치**하세요: 시트 메뉴 `📚 도서관 관리 → 관리 → 일일 관리
   트리거 설치`를 다시 실행하면(기존 트리거를 삭제하고 재생성) `dailyLibraryMaintenance`(오전 4시)와
   `dailyVizBatch`(오전 5시) 트리거가 함께 설치됩니다. **트리거 설치 직후엔 아직 `20_VIZ_CACHE`가
   비어있으므로 `viz` 액션은 `VIZ_NOT_READY` 오류를 반환하고 여전히 샘플로 보입니다** — 오전 5시가 한 번
   지나거나, Apps Script 편집기에서 `dailyVizBatch` 함수를 수동으로 한 번 실행하면 즉시 채워집니다.

**「샘플 데이터」 배지가 사라지는 것으로 재배포 성공을 확인하세요** — 각 화면을 새로고침(또는 수동 새로고침
버튼)했을 때:
- [ ] 대시보드 KPI 카드·조용한 신호 결과에서 배지가 사라지고 실제 시트 숫자로 바뀐다
- [ ] 리포트(미대출 학생 발굴·담임 리포트) 미리보기에서 배지가 사라지고 실제 학생·책 이름이 보인다
- [ ] 시각화 4종에서 배지가 사라진다(단, `dailyVizBatch`를 아직 안 돌렸다면 이 하나만 `VIZ_NOT_READY`로
  계속 샘플일 수 있음 — 위 3번 참고, 다른 화면이 실데이터로 바뀌었는데 시각화만 계속 샘플이면 정상 상태이지
  재배포 실패가 아니다)
- [ ] 배지가 안 사라진다면: 브라우저 캐시/서비스워커 문제일 수 있으니 완전 종료 후 재실행, 그래도 안 되면
  Apps Script 배포 URL이 실제로 최신본을 가리키는지 배포 관리 화면에서 재확인

동작 확인 절차(대출·반납, 기존 절차 유지):
- [ ] 대출 가능한 책 스캔 → 서명 표시 + "누가 빌리나요?" → 학생증 스캔 → "대출 완료" 토스트 + 5초 실행취소
- [ ] 방금 대출한 책을 다시 스캔 → (재조회 후) **즉시 반납** 처리 + 누가 빌렸었는지 표시
- [ ] 실행취소를 눌러 반대 트랜잭션이 성공하는지 (반납의 취소 = 같은 학생으로 재대출)
- [ ] `10_LOANS` 시트에서 해당 대출 행의 `note`에 `웹앱 · <작업자>`가 남는지 (작업자 추적)
- [ ] LOST/REPAIR 상태 소장본을 스캔하면 "대출할 수 없는 상태" 오류가 뜨는지
- [ ] 대출·반납 트랜잭션 직후 대시보드가 자동으로 새로고침되는지(수동 새로고침 없이 숫자가 바뀌는지 —
  "트랜잭션 후" 갱신 트리거 확인, todo/04)

## 4½. 디자인/UX 일괄 수정 후 아이폰 설치형 재확인 (2026-07-15 반영분)

safe-area·아이콘·토스트 위치·타입 스케일이 바뀌었으므로 **설치형 PWA에서 아래를 다시 확인**하세요.
브라우저 탭이 아니라 **홈 화면 아이콘으로 열어야** safe-area가 실제로 검증됩니다(탭에서는 인셋이 0).

1. **이미 홈 화면에 추가돼 있던 경우**: iOS가 구버전 에셋을 캐시할 수 있습니다 — 앱을 완전히 종료 후
   재실행하고, 그래도 예전 화면이면 아이콘 삭제 → Safari 방문 → 홈 화면에 다시 추가.
2. **상단 잘림(핵심)**: 헤더("스캔"/"도서 등록" 타이틀 줄)가 **시계·노치 아래**에 온전히 보이는지.
   스택 화면(도서 상세 등)을 열었을 때 "‹ 뒤로" 헤더도 마찬가지.
3. **가로 회전**: 가로로 돌렸을 때 탭바·헤더·더보기 목록이 노치 쪽에 잘리지 않는지(좌우 safe-area).
4. **아이콘**: 탭바·더보기·설정(⚙ 대신 선형 아이콘)·도서 등록 헤더가 전부 **선형(lucide) 아이콘**으로
   보이는지 — 이모지가 하나라도 남아 보이면 알려주세요.
5. **토스트 위치**: 대출·반납에서 성공/실패 토스트가 **탭바 위**에 뜨는지(탭바에 가리면 실패).
   데스크톱에서는 우하단 **스캐너 도크 위**에 뜨는지. 카메라 "곧 꺼집니다" 유휴 경고 토스트도 같은 위치인지.
6. **바텀시트**: (사용되는 화면이 생기면) 시트 하단 콘텐츠가 홈 인디케이터에 가리지 않는지.
7. **초기 설정 화면**: 공유 토큰 입력칸 안내문이 "사서 선생님께 받은 공유 토큰"으로 보이는지,
   노치에 카드가 가리지 않는지.
8. **접근성**: 외장 키보드(또는 데스크톱)에서 Tab 이동 시 **황동색 포커스 링**이 보이는지.
   설정 → 손쉬운 사용 → 동작 줄이기 켠 상태에서 화면 전환 애니메이션이 사실상 없는지.

## 5. 기존 배포 철수 계획 — 지금은 1~2단계까지만

- ✅ 1단계(병행 배포): `/app/`에 새 앱, 기존 스파이크·`register.html`은 그대로.
- ⏳ 2단계(검증): 이 문서의 절차로 사용자가 실기기에서 확인.
- ⛔ 3단계(리다이렉트 스텁으로 교체)는 **아직 하지 않았습니다** — 지시대로 승인 대기 중입니다. 2단계가
  끝나 확실해지면 알려주세요.

## 6. 수용 기준 체크리스트 (FRONTEND.md·ADR-023 원문)

- [x] `views/**`에서 `matchMedia|innerWidth|window\.(?!location)` grep 0건 + 셸 import 0건 (CI 린트) — 자동 확인됨
- [x] `views/**·shells/**·student/**` JSX 한글 리터럴 0건 (CI 린트, todo/02) — 자동 확인됨
- [x] `getUserMedia` 호출 지점 정확히 1곳(`camera.ts`) — 자동 확인됨(todo/03 온디맨드 정책 계층 추가 후도 유지)
- [x] 번들 예산 work≤180KB/student≤70KB gzip (CI, todo/02) — 자동 확인됨, 실측치는 위 "번들 예산 실측 기록" 표
- [ ] 같은 뷰 파일이 두 셸에서 렌더 — 데스크톱 창/모바일 탭 스크린샷 비교 → **사용자 육안 확인 필요**(헤드리스로
      렌더는 확인했으나 "같은 화면인지" 비교는 스크린샷을 보고 판단하는 항목이라 남겨둠)
- [x] 창 위치·크기 재접속 복원, 최소화·스냅·z-순서 동작 — 자동 확인됨(스냅/z-순서는 정적 코드 리뷰 + 일부 자동화, 전체는 위 1절 체크리스트로 재확인 권장)
- [ ] 포커스 창 전환 시 스캔이 새 포커스 창으로 감(핀 시 핀 창) — **실제 카메라로 확인 필요** (위 1-5절)
- [ ] 카메라 온디맨드: 3가지 시작 트리거·유휴 3분·연속 모드가 두 셸 모두에서 동작 — **실제 카메라로 확인
      필요** (위 1-4절·2-4절, ADR-020/todo/03)
- [x] 학생 번들: `/b/` 진입 시 사서 셸 코드 미로딩 — 자동 확인됨(네트워크 요청에 DesktopShell/MobileShell 청크 없음)
- [ ] iOS 설치형 PWA에서 스캔 연속 20건 — **실기기 필요** (위 2절)
- [ ] 창을 모두 닫으면 대시보드가 항상 보임, 모바일 더보기 상단 요약, 샘플 배지 동작 — **실기기 확인 필요**
      (위 1-2절·4절, todo/04)
- [ ] 리포트 인쇄 미리보기가 A4로 정상 출력, 「조용한 신호」에서 직행 — **실기기 확인 필요**(위 1-6절, todo/05)
- [ ] 시각화 4종이 대시보드·reports에 착륙, 램프 외 색 0, 지연 로딩 체감 — **실기기 확인 필요**(위 1-7절,
      정적 코드로는 "램프 외 색 0"만 grep으로 자동 확인됨, 실제 렌더 색상 육안 확인은 남음, todo/06)
